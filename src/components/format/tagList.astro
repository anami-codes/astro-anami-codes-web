---
import { getLangFromUrl } from '../../i18n/utils';

const lang = getLangFromUrl(Astro.url);

const { tags, color } = Astro.props;
const tagList = tags.map( (tag: any) => 
		{return `<a href="/${lang}/tags/${tag}" class="btn tag ${color}">🌙${tag}🌙</a>`}
	)

---

<Fragment set:html={tagList}` />