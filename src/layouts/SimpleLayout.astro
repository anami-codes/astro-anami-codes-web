---
import BaseLayout from "./BaseLayout.astro";

const {title, banner, navCode, url, lang} = Astro.props;
---

<BaseLayout title={title} banner={banner} navCode={navCode} url={url} lang={lang}>
    <div style="min-height: 80vh;">
    <slot />
    </div>
</BaseLayout>